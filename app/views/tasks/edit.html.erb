<%= form_with model: @task do |f| %>

<div class="form-group">
  <label for="task_name"></label>
  <%= f.text_field :name %>
</div>

<table class="tasks" border="1" style="border-collapse: collapse">
  <tr align="center">
    <td>開始</td>
    <td>
      <div class="form-group">
        <label for="task_start"></label>
        <%= f.date_field :start %>
      </div>
    </td>
  </tr>
  <tr align="center">
    <td>期限</td>
    <td>
      <div class="form-group">
        <label for="task_limit"></label>
        <%= f.date_field :limit %>
      </div>
    </td>
  </tr>

  <tr align="center">
    <td>ご褒美</td>
    <td>
      <div class="form-group">
        <label for="task_present"></label>
        <%= f.date_field :limit %>
      </div>
    </td>
  </tr>

  <tr align="center">
    <td>メモ</td>
    <td>
      <div class="form-group">
        <label for="task_present"></label>
        <%= f.text_field :note %>
        <%= f.file_field :image, accept: "image/*" %>
      </div>
    </td>
  </tr>

</table>

<%= f.submit "変更を保存" %>

<%= link_to "削除",task_path(task), method: :delete, "data-confirm" => "削除しますか？",class:"btn btn-sm btn-danger destroy_book_94226" %>

<% end %>