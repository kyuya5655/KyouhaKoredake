<h3><%= current_user.name %>さん</h3>
<p>
  お疲れさまです。<br />
  今日（<%= l @today, format: :short %>）のタスクは( <%= @task_today_counts %> )件です。今日はこれだけ頑張りましょう！
</p>
<p><%= image_tag 'あなた次第.png', size: '300x300' %></p>

<table class="tasks" border="1"  style="border-collapse: collapse">
  <tr align="center">
    <th>ステータス</th>
    <th>タスク名</th>
    <th>ご褒美</th>
  </tr>
  <tr align="center">
     <% @task.each do |task| %>
      <td><%= task.status_i18n %></td>
      <td><%= link_to task.name, edit_task_path(task.id) %></td>
      <td><%= task.present %></td>
    <% end %>
  </tr>
</table>

<div class="notcompleted_task_introduce">
  未完了タスクがございます。。。ご確認ください！m(_ _)m
</div>

<table class="tasks" border="1"  style="border-collapse: collapse">
  <tr align="center">
    <th>タスク名</th>
    <th>期日</th>
  </tr>
  <tr align="center">
    <tr align="center">
     <% @task_not_completed.each do |task_not_completed| %>
      <td><%= link_to task.name, edit_task_path(task.id) %></td>
      <td><%= l task.limit %></td>
    <% end %>
  </tr>
  </tr>
</table>

<p>
  <%= link_to "明日以降のタスクを確認するー＞", tasks_path %>
</p>